# Brainstorms

Exploratory thinking documents created with `/ce:brainstorm` before planning new features. Each file captures the "what and why" before the "how". Run `/ce:plan` to convert a brainstorm into an actionable plan.
